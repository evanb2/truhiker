import { DefaultTheme } from 'react-native-paper'
/**
 * Paper UI Theme
 */
export const theme = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
    primary: 'tomato',
    accent: 'yellow',
  },
}
